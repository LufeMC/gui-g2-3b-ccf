# vLLM official Docker image (vllm/vllm-openai:vX) brings torch +
# transformers + flash-attn + xformers transitively pinned. We only
# install the FastAPI-level deps on top of that base image. This file
# is kept for non-Docker development setups (local venv, RunPod
# bootstrap), where you would install vllm separately.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
Pillow>=10.0.0
